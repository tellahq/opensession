# Pstack child brief

Begin the child session prompt with `/pstack`. Include the task, scope, relevant paths, acceptance criteria, forbidden actions, exact verification, and report shape. The child must inspect current state first, follow repository instructions, and return evidence rather than a bare claim.

Use ask mode for read-only work. Use code mode with an isolated worktree for independent writes. Do not ask multiple children to edit the same files or branch.
